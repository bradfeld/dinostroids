{
  "dependencies": {
    "@vercel/analytics": "^1.5.0",
    "@vercel/kv": "^3.0.0",
    "http-server": "^14.1.1",
    "next": "^14.1.0"
  },
  "name": "dinostroids",
  "version": "1.0.0",
  "description": "A classic arcade-style shooter game built with HTML Canvas and pure JavaScript, done entirely with vibe coding in Cursor.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.js --bundle --outfile=bundle.js --format=esm --minify",
    "dev": "esbuild src/index.js --bundle --outfile=bundle.js --format=esm --watch --servedir=.",
    "serve": "http-server ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bradfeld/dinostroids.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/bradfeld/dinostroids/issues"
  },
  "homepage": "https://github.com/bradfeld/dinostroids#readme",
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
